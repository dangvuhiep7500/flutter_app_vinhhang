import 'package:flutter/material.dart';

class CardHomie extends StatelessWidget {
  const CardHomie({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
