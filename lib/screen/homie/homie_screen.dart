import 'package:flutter/material.dart';
import 'package:flutter_app_vinhhang/screen/homie/components/body.dart';

class HomieScreen extends StatelessWidget {
  const HomieScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Body(),
    );
  }
}
